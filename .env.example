# URL your form POSTs to. Leave unset to use the built-in /api/waitlist route.
NEXT_PUBLIC_WAITLIST_ENDPOINT=

# MongoDB Atlas connection string, with the real password inline.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=<app>
MONGODB_DB=waitlist
MONGODB_COLLECTION=emails
